### VAL-ALERT-001: A failed deployment notifies a person

When a deployment fails, a notification reaches a real inbox naming the app and the
failure, without anyone watching the dashboard.
Tool: a deliberately failing deploy on staging
Evidence: the received notification, and the deployment it corresponds to
